<h1>Listing ranks</h1>

<table>
  <tr>
    <th>Rank</th>
    <th>Ordertype</th>
    <th>Rating</th>
    <th>Downloadcount</th>
    <th>App</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ranks.each do |rank| %>
  <tr>
    <td><%= rank.rank %></td>
    <td><%= rank.orderType %></td>
    <td><%= rank.rating %></td>
    <td><%= rank.downloadCount %></td>
    <td><%= rank.app_id %></td>
    <td><%= link_to 'Show', rank %></td>
    <td><%= link_to 'Edit', edit_rank_path(rank) %></td>
    <td><%= link_to 'Destroy', rank, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Rank', new_rank_path %>
